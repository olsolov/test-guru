en:
  activerecord:
    models:
      test: 'Test'
      question: 'Question'

    attributes:
      test:
        title: 'Title'

    errors:
      messages:
        blank: "can't be blank"
#        required: 'не указан(а)'
#        record_invalid: 'Возникли ошибки: %{errors}'
